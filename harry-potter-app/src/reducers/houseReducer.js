const initialState = {
  isFetching: false,
  houses: [],
  error: ''
}

export const houseReducer = (state = initialState, action) => {
  switch(action.type) {
    default: 
      return state;
  }
}